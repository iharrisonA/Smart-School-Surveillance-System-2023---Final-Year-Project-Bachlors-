@{ ViewData["Title"] = "Add User"; ViewData["Active"] = ""; }
<div class="card" style="max-width:500px">
    <div class="card-header"><i class="bi bi-person-plus me-2"></i>Add User Account</div>
    <div class="card-body">
        <p class="text-muted">Use <a asp-action="AddStudent">Add Student</a> or <a asp-action="AddTeacher">Add Teacher</a> to create user accounts with the appropriate role.</p>
    </div>
</div>
